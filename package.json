{
  "name": "quiz-app",
  "version": "2.0.0",
  "description": "Project to quickly get something working in typescript",
  "scripts": {
    "start": "run-p compile serve",
    "compile": "tsc --watch",
    "build": "tsc",
    "serve": "live-server --watch=index.html,build --port=5500 --wait=100"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.1.2"
  }
}
