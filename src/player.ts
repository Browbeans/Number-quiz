//Player class

class Player {
    private name: string;
    private avatarPath: string;

    constructor(name: string, avatarPath: string) {
        this.name = name;
        this.avatarPath = avatarPath;

    }
}